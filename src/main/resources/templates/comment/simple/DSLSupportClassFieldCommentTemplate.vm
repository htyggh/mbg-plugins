## DSL支持类 字段简单注释模版
/**
 * 字段描述：${tableName}表${columnName}列，列对象#if($isPK) [主键]#elseif($isNull) [可空]#{else} [不可空]#end#if($isAutoIncrement) [自增]#end [默认值：#if(!$defaultValue)无默认值]#{else}${defaultValue}]#end
 */